import styled from 'styled-components'
import tw from 'tailwind-extended.macro'

export const SpOnlySpan = styled.span`
  ${tw`hidden sm:inline`}
`
export const SpOnlyBr = styled.br`
  ${tw`block sm:hidden`}
`
